//Function that will do something
function doSomething(z) { /*This is a valid line with a comment*/
  /*
     let val = z / 2;
     return val;
  */
}

let x = 1;
let y = 4;
x = doSomething(y); // "X" would be undefined; may not be desirable
